Paper-local inputs for the sequence-mode parameter objective tradeoff figure.

`exact_scores.csv` is the corrected exact selected-feature score table. The
tradeoff figure uses `bedtools_pearson` for agreement with exact coordinate
Jaccard and `ari_10class` or `ari_8class` for tissue-label recovery.
